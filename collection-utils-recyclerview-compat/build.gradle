buildscript {
    dependencies {
        classpath "com.jfrog.bintray.gradle:gradle-bintray-plugin:$vBintray"
        classpath "com.github.dcendents:android-maven-gradle-plugin:$vMavenGradle"
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 29
    buildToolsVersion "29.0.3"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 29

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    jcenter()
    mavenCentral()
    maven { url 'https://maven.google.com/' }
}

dependencies {
    implementation "androidx.recyclerview:recyclerview:1.1.0"

//    releaseImplementation project(path: ':collection-utils')
//    debugImplementation project(path: ':collection-utils')
    implementation project(path: ':collection-utils')
    implementation project(path: ':collection-utils-recyclerview')
    implementation project(path: ':collection-utils-listview')

    testImplementation 'junit:junit:4.12'
}

ext {
    bintrayRepo = 'maven'
    bintrayName = 'collection-utils-recyclerview-compat'

    publishedGroupId = 'com.guardanis'
    libraryName = 'collection-utils-recyclerview-compat'
    artifact = 'collection-utils-recyclerview-compat'

    libraryDescription = 'An assortment of modularized additives for collection-based layouts (RecyclerView compatibility components for ListView)'
    libraryVersion = '4.0.0'

    developerId = 'mattsilber'
    developerName = 'Matt Silber'
    developerEmail = 'matt@guardanis.com'
}

apply from: 'https://raw.githubusercontent.com/attwellBrian/JCenter/master/installv1.gradle'
apply from: 'https://raw.githubusercontent.com/attwellBrian/JCenter/master/bintrayv1.gradle'
